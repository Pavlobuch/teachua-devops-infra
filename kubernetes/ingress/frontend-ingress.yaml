apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teachua-ingress
  namespace: teachua
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: frontend
              port:
                number: 80